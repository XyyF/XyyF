module.exports = [
    {
        sshAddress: 'git@github.com:XyyF/elfin-algorithm.git',                   /* git ssh地址 */
        // branch: 'main',              /* clone的分支，默认master */
        skipInstall: true,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfin-commander.git',                   /* git ssh地址 */
        // branch: 'main',              /* clone的分支，默认master */
        skipInstall: false,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfin-dataStructure.git',                   /* git ssh地址 */
        // branch: 'main',              /* clone的分支，默认master */
        skipInstall: false,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfin-demo.git',                   /* git ssh地址 */
        branch: 'main',              /* clone的分支，默认master */
        skipInstall: true,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfin-eslint.git',                   /* git ssh地址 */
        branch: 'main',              /* clone的分支，默认master */
        skipInstall: false,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfin-finance.git',                   /* git ssh地址 */
        branch: 'main',              /* clone的分支，默认master */
        skipInstall: true,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfin-utils.git',                   /* git ssh地址 */
        branch: 'main',              /* clone的分支，默认master */
        skipInstall: false,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfin-vscode.git',                   /* git ssh地址 */
        // branch: 'main',              /* clone的分支，默认master */
        skipInstall: false,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfinUI.git',                   /* git ssh地址 */
        // branch: 'main',              /* clone的分支，默认master */
        skipInstall: false,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfin-schedule.git',                   /* git ssh地址 */
        branch: 'main',              /* clone的分支，默认master */
        skipInstall: false,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfin-inquirer-search-list.git',                   /* git ssh地址 */
        // branch: 'main',              /* clone的分支，默认master */
        skipInstall: false,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:fe-interest-group/regexp-exercise.git',                   /* git ssh地址 */
        branch: 'main',              /* clone的分支，默认master */
        skipInstall: true,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:fe-interest-group/elfin-animation.git',                   /* git ssh地址 */
        // branch: 'main',              /* clone的分支，默认master */
        skipInstall: true,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:XyyF/elfin-draft.git',                   /* git ssh地址 */
        branch: 'main',              /* clone的分支，默认master */
        skipInstall: true,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: 'git@github.com:fe-interest-group/elfin-figma-brain-plugin.git',                   /* git ssh地址 */
        branch: 'main',              /* clone的分支，默认master */
        skipInstall: true,             /* 是否跳过install依赖，默认为false */
    },
    {
      sshAddress: 'git@github.com:XyyF/pic_bed.git',                   /* git ssh地址 */
      branch: 'main',              /* clone的分支，默认master */
      skipInstall: true,
    }
]